<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="itemViewModel"
            type="com.example.newsapp.viewmodel.ItemCardViewModel"/>
        <variable
            name="viewmodel"
            type="com.example.newsapp.viewmodel.HomeViewModel" />
    </data>
   <androidx.cardview.widget.CardView
       xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:app="http://schemas.android.com/apk/res-auto"
       android:layout_height="250dp"
       android:layout_width="match_parent"
       android:background="@color/black"
       app:cardElevation="10dp"
       app:cardCornerRadius="10dp"
       app:cardPreventCornerOverlap="true"
       app:cardUseCompatPadding="true">
       <androidx.constraintlayout.widget.ConstraintLayout
           android:layout_width="match_parent"
           android:layout_height="match_parent">
          <ImageView
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               app:layout_constraintTop_toTopOf="parent"
               app:layout_constraintStart_toStartOf="parent"
               app:imageUrl="@{itemViewModel.component1()}"/>

           <TextView
               android:id="@+id/header_news"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               android:text="@{itemViewModel.component1()}"
               android:layout_marginBottom="24dp"
               app:layout_constraintStart_toStartOf="parent"
               app:layout_constraintBottom_toBottomOf="@id/news_id"/>

           <TextView
               android:id="@+id/news_id"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               android:layout_marginBottom="12dp"
               android:text="@{itemViewModel.component2()}"
               app:layout_constraintTop_toBottomOf="@+id/header_news"
               app:layout_constraintBottom_toBottomOf="parent"
               app:layout_constraintStart_toStartOf="parent"/>
           <TextView
               android:id="@+id/date"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               android:layout_marginBottom="12dp"
               android:text="@{itemViewModel.component3()}"
               app:layout_constraintBaseline_toBaselineOf="@id/news_id"
               app:layout_constraintStart_toEndOf="@id/news_id"/>
       </androidx.constraintlayout.widget.ConstraintLayout>
   </androidx.cardview.widget.CardView>
</layout>
